#%SED_GAWK_PATH%

# ------------------------------------------------------------
# cawk is subject to a MIT open-source licence
# please refer to the MIT licence file for further information
#
#
# for %SED_VAR% change like GAWK_PATH, etc. please refer to
# file support/tests.sed for further information
#
# @test_name : snmp_v1v2_notset.paloalto-panos.gawk
# @supplier : paloalto-panos
# @purpose : check if <snmp v1v2> is not set
# @description : snmp v1 and v2c use cleartext community strings and lack authentication, making them insecure
# @actions : disable snmp v1 and v2c and migrate to snmp v3 with authentication and encryption (please refer to engineering/operations guidelines for further information)
# @cis_benchmark_ref : 1.6.1 | Ensure SNMPv3 is used | SNMP
# @authors : cedric llorens
# ------------------------------------------------------------

@include %SED_INCLUDE_PATH%

BEGIN {
}

BEGINFILE {
	pass = 1;
}

/^%SED_BK_PALO%system/ {
	block_system = 1;
	next;
}

/^%SED_BK_PALO%{2}snmp/ && block_system == 1 {
	block_snmp = 1;
	next;
}

/^%SED_BK_PALO%{3}community/ && block_snmp == 1 {
	print_err("snmp_v1v2_notset.paloalto-panos.gawk",rm_blank_before($0),FNR,"high","error");
	pass = 0;
	nextfile;
}


/^%SED_BK_PALO%{2}\}/ && block_snmp == 1 {
	block_snmp = 0;
	next;
}

/^%SED_BK_PALO%\}/ && block_system == 1 {
	block_system = 0;
	next;
}

ENDFILE {
	if ( pass ) {
		print_err("snmp_v1v2_notset.paloalto-panos.gawk","snmp v1v2 is not set",0,"high","pass");
	} 
}

END {
}
